// ============================================================
// THEME: INDEX (Barrel File)
// ============================================================
// Import theme stuff like:
//   import { themes, themeList, useTheme } from '@theme/index';
// ============================================================

export { themes, themeList, lightTheme, darkTheme, midnightTheme, sunsetTheme, DARK_MAP_STYLE } from './themes';
export type { ThemeColors, AppTheme, ThemeName } from './themes';

// Legacy static colors (kept for backward compatibility / reference)
export { colors } from './colors';
export { typography } from './typography';
export { spacing, borderRadius, layout, shadows } from './spacing';
